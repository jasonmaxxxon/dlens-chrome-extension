// Fixture facade for tests/motion-registry.test.ts's family-guard proof.
// Deliberately clean — the violation lives in the nested sibling below, not
// here, so a guard that only reads this file never finds it.
export function FacadeView() {
  return null;
}
