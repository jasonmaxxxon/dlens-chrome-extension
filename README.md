# DLens Chrome Extension

DLens is a mode-aware MV3 Chrome extension for capturing Threads posts and turning them into research, product-signal, and PR evidence workflows.

> Last updated: 2026-07-21
> Current source version: `0.3.57` · Product Action now uses one decision-summary column beside one evidence column; selected brief chips and the stage grid can shrink without pushing the workspace beyond its frame
> Release state: source `0.3.57` is local-only; it has not been pushed or tagged. Static gates, the MV3 build, and Real-Chrome reload/manual acceptance in Jason's `Default` profile on a real Threads page all pass.
> Positioning (2026-06-18, design contract amended 2026-07-10): local power-tool (self + small technical circle); two separate repos (extension public · ingest-core **private**), not monorepo; design contract is `src/ui/tokens.ts` glass workspace (warm ink + serif voice on the shared `workspaceGlass` white ground — the warm-paper cream surface family is retired)
> Load-unpacked build path: `output/chrome-mv3`
> Stability note: `TRACE`, `SEAM_GUARD`, `RECONCILE`, `INVALIDATE`, `BOUNDARY`, and `MIGRATE` are locked in the live architecture map. Visual Reset A shipped the native-feeling shell plus PR Evidence, Topic, Compare, and Product marquee surfaces without changing storage, backend, ViewModel, command, or signal-packet contracts.
> Runtime note (2026-07-10): Chrome reads `output/chrome-mv3`, not raw `src/`. Topic/Product/PR now share one glass workspace shell AND one glass surface family — as of 0.3.28 every inner card (rail, Collect, Settings, Topics list, Product signals) sits on the same white-green ground as the shell; cream paper no longer appears anywhere. Topic Audit keeps the current Signal Atlas mounted while regeneration runs, replaces the legacy non-ready overview with a compact in-frame state, and no longer presents memo-derived `Pn/6` as live progress.

## What It Does

DLens is extension-first, not SaaS-first. Local folders, UI state, product context, user API keys, saved analyses, Product signal judgments, SignalReading records, and PR evidence rows live in Chrome/local extension storage. The optional ingest backend owns crawl jobs and canonical read models.

Current workspace modes:

| Mode | Main use | Current status |
|---|---|---|
| `archive` | Save and inspect Threads posts locally | Working |
| `topic` | Triage saved posts into topics, compare ready pairs, attach results to case context | Working |
| `product` | Analyze saved signals against ProductContext, review free-text readings, export agent-readable packets | Working |
| `pr-evidence` | Read the current campaign narrative and build evidence ledgers from manually collected Threads posts | Working |

## Recent Month Contract

Recent PRs and local commits changed the shape more than the old README suggested:

- PR #16-#20 moved Product, Topic, Compare, and PR Evidence views behind ViewModel/read boundaries.
- PR #21-#29 locked the typed trace spine through a full live backend/direct-LLM fixture.
- PR #30 and #38 locked storage writes behind seam guards; PR #40-#45 locked reconcile/invalidation terminal behavior.
- PR #46-#51 locked View/ViewModel boundary guards and storage migration fixture coverage.
- PR #53-#57 shipped Signal Packet HTML/source/lineage provenance and 0.2.1.
- PR #58-#67 shipped Visual Reset A and release 0.3.0.
- PR #68-#71 handled post-reset LOC/overflow fixes: Library/Compare split, popup clipping, topic removal, masthead containment.
- Local 0.3.6/0.3.7 commits and worktree changes compact Product saved-signal rows, suppress stale backend errors, restore the extension action side panel, and move Product classification visibility into the Saved Signals surface.
- Local 0.3.45-0.3.49 commits (2026-07-14–17) shipped model transport fixes (stable Gemini ID, Claude Sonnet 5, Retry-After), the S2-S5 topic truth contract, S7 single-poller status sync, S6 PR narrative semantic repair, Wave 3 render hot-path hardening, the 0.3.48 timeout/storage-integrity fixes, and 0.3.49 Topic session envelope resilience.

## Current Features

- Hover-to-preview and collect on Threads feeds/post pages. Hover uses a warm in-memory cache (no per-move storage read); saves carry the live hovered post and the popup's visible folder/topic so they always land where intended. Collect metrics use shared icon chips across the overlay and popup preview. Collect saves and refresh-all writes share the snapshot lock so Topic signals cannot be left without a usable backing saved item/descriptor; pre-existing orphan/corrupt signals are hidden from Topic counts/lists and queued for storage cleanup. Content scripts also rehydrate active collect mode after extension reload/page refresh (see AGENTS.md "Recently Fixed 2026-05-22").
- Mode-aware save routing: archive saves to Library; topic/product saves become Inbox signals; PR Evidence saves become campaign rows. Save messages now pass an explicit `sessionId`/`topicId`, so a drifted active folder cannot reroute a save.
- Backend queue/drain/polling against `ingestBaseUrl`, defaulting to `http://127.0.0.1:8000`.
- Popup backend health dot uses the lightweight backend `/health` endpoint for liveness, while `/worker/status` remains the slower work-truth projection for backlog / retry / analysis state. One failed health poll is suppressed, two mark the dot slow, and three mark it unreachable.
- Compare setup and Result surfaces with backend read models plus extension-side compare brief v8, cluster summaries, evidence annotations, and saved analysis snapshots.
- Topic workflow: Casebook, Inbox, Topic Detail, signal triage, per-signal semantic tags/gists, optional-question TopicSignalReading, and attached compare pairs.
- 0.3.52 Atlas Refit C: P3 v4 may add optional evidence-bound beats (`setup` / `tension` / `outcome`) and `new` / `carried` trajectory; the narrative stage is paged, the compass reports assignment distribution, and the ready source disclosure starts closed.
- Product workflow: ProductContextCompiler, ProductSignalAnalyzer v17, a compact Saved Signals intake, a staged Product Action review, Signal Packet export support, and local feedback history. v17 stops asking the model for legacy action-recipe fields such as `copy_recipe_markdown` / `workflow_stack`; Product Action keeps evidence as reusable patterns plus agent briefs, not tutorial recipes.
- Product Saved Signals is the current landing surface and a compact lifecycle intake: collected, ready to analyze/retry, processing, and complete counters plus a default-collapsed source-management disclosure. It owns neither readings, verdict navigation, nor any export surface. `classification` remains an allowed internal/deep-link page but is not rail-visible.
- Product Action owns one large stage and text-labelled previous/next pager. Four color-and-text verdict tiles (`try`, `park`, `insufficient`, `watch`) switch the active bucket; every bucket uses the same stage geometry, while park/insufficient show a semantic exclusion or evidence-gap reason instead of actionable reading/review controls. Dots, the macro scoreboard, multi-card wall, and duplicate exclusion lanes are removed. Forward/backward stage changes use token-owned x motion only under `prefers-reduced-motion: no-preference`, while reduced motion explicitly clears animation and transforms.
- Product Action alone owns the selected action-brief shelf and the one whole-folder HTML/JSONL packet-export shelf. Saved Signals has no brief, packet, or batch export UI; reading generation and review remain inside the active actionable stage.
- Signal Packet export: Product sessions can export `DLensSignalPacket` records as HTML, Markdown, or JSONL through `signal-packet/export`. The 0.2.1 surface adds compact HTML density, source/capture/item provenance, reading citation refs, filed-reading lineage, source URL provenance, and a Product reading-review provenance mirror. These fields are additive on packet v3; no storage migration is required.
- Visual Reset A (amended 0.3.28): the popup shell, PR Evidence ledger, Topic detail, Compare hero, and Product action surfaces use the `src/ui/tokens.ts` contract with macOS utility shell affordances; since 0.3.28 the surface family is the glass-white ground (warm-paper values retired). VIEW remains 🟢, not 🟩: marquee surfaces are DOM-test-locked, while row-level primitive adoption / LOC reduction remains follow-up work.
- PR Evidence workflow: one active campaign per session; editable brief, core narrative, target audience, desired action, and six matching criteria; manually collected evidence rows; current-only Narrative and Evidence lenses; criteria matching; CSV export; Markdown/DOCX audit summary. Narrative generation is manual and reads only that campaign's collected Threads main-post text.
- Layout preferences remain persisted for existing records, but the visible Settings layout and folder-mode controls are removed; workspace typography, rounded surfaces, and shadow treatment now follow the Topic card grammar across modes. Shared cards default to the 20px Topic-style radius.
- Workspace mode switches reserve the processing-strip slot, reset scroll before paint, and crossfade the content frame so Topic/Product/PR data changes no longer produce a visible vertical jump.
- 0.3.2 UI fix: compact masthead now removes nonessential shortcut/issue chrome, keeps only status + version on the right, and Topic cards/buttons stay inside the content width with a non-overlapping remove action.
- 0.3.1 UI fix: popup masthead status/key hints now shrink inside the 720px shell, Product classification detail text wraps instead of clipping, and Topic cards expose an explicit remove action.
- 0.3.7 local UI fix: Product saved-signal classification is merged into Saved Signals instead of appearing as a separate rail stop; the source contract is guarded by `getModeRailPages("product") === ["saved-signals", "actionable-filter", "collect"]`.
- 0.3.13 local UI fix: Topic Collect's 未分流 queue exposes per-row and bulk delete through `signal/delete`, and Topic/Product/PR modes render no top folder/topic strip. Topic destination choice lives in the floating collect preview card; topic creation lives in the 議題 page.
- 0.3.18 local UI update: Topic Audit renders the Signal Atlas L0 reading spine with shared evidence ref chips, cross-post narrative strength, counts-only source footer, one unified right-side drawer, and shard-backed full-list expansion.
- 0.3.19 local UI fix: Topic Audit ready state drops the duplicate overview header and keeps only the report/regenerate action row; the L0 detail drawer is contained to the extension frame instead of the Threads viewport.
- 0.3.22 local UI update: Topic/Product/PR use one Variant-D-derived glass shell across the popup canvas, masthead, rail, and main frame. Topic regeneration keeps the last Atlas visible inside the same glass frame; first-run/failed/stale states no longer reintroduce the legacy overview or memo-derived stage progress. Atlas counts now distinguish captured/read/usable comments from overlapping pattern assignments.
- 0.3.23 local UI fix: the L0 detail drawer re-anchors to the user's current viewport sightline when it opens (and tracks scroll/resize), instead of sitting at the top of the tall frame-contained shell; the 缺席與可靠性 strip is redesigned as an amber glass card with a serif reading voice, a caveat count, and bulleted limits.
- 0.3.24 local UI fix: clicking a 民情羅盤 bubble no longer draws the browser's default rectangular focus box; the SVG bubble suppresses the mouse-focus outline and shows a circular signal-coloured focus ring only on keyboard `:focus-visible`.
- 0.3.25 local UI update: the 缺席與可靠性 strip is rebuilt as two labeled epistemic zones (`data-reliability-zone` 缺席的聲音 / 可靠性限制) that separate what the discourse omits from the reading's own caveats, each with an icon header; the atlas canvas gains bottom breathing room so the last card no longer sits flush against the frame edge. New token `color.queuedDeep`.
- 0.3.26 local UI fix: the atlas canvas bottom padding is increased to 48px (from 24px) so the last L0 card — the reliability strip — floats clearly above the canvas edge instead of reading as clipped; the shell's own scroll-bottom spacer already handled space below the canvas, but the gap inside the canvas was too tight.
- 0.3.27 local UI fix: the outer popup frame and its border-box scroll mask restore Variant D's 28px radius, so the main-frame divider turns through the visible bottom curve instead of being clipped into the page edge. Narrow viewports clamp the popup inside the window; Atlas drawer scroll positioning and bubble accessibility are hardened in the same pass.
- 0.3.28 local UI update: the editorial warm-paper surface family is retired (user direction, 2026-07-10). `tokens.ts` `color.canvas/surface/elevated` and the shell/rail/context/utility/neutral/idle/disabled surface roles are retuned to whites derived from the `material.workspaceGlass` canvas stops, so the rail, Collect, Settings, Topics list, and Product Saved Signals inherit the 0.3.22 glass grammar without view changes; the `data-paper-grain` texture overlay is removed (one-in-one-out: grain CSS + two attributes out, zero surface added). Warm ink, serif headline voice, mode accents, and natural-dye status colors are unchanged; `tokens-intent.md` is amended in the same change.
- 0.3.57 Product Action hierarchy + overflow fix: the active stage now uses a Topic-like split between one quiet decision summary and one source-evidence stack, with the brief-selection state moved into the header. Duplicate reason/takeaway copy is collapsed and a next action renders only when backed by `agentTaskSpec` or `experimentHint`; source truth, exact quote, deep reading, export ownership, and the four-verdict navigation contract remain intact. The actual overflow cause was not the aura: opening the brief shelf let a no-wrap selected-summary chip contribute its intrinsic width to an auto grid track. The content track and export body now use `minmax(0, 1fr)`, details and summary wrappers own `min-width: 0`, and selected chips use a shrinkable flex basis plus border-box sizing. Gate: 1328 tests / 1323 pass / 5 skipped / 0 fail; typecheck, boundary/storage/code-path guards, production build, bundle guard, and diff check pass. `output/chrome-mv3/manifest.json` is `DLens v3` `0.3.57` with SHA-256 `c6114fdc68cef04b38e18740c10cd93ab13d7d0d3188711b871427d8a6a9c92c`; the manifest-referenced `content-scripts/threads.js` is 891804 raw / 251041 gzip -9 / 200023 brotli bytes with SHA-256 `13c7d63b8c046f900936ff9afa382833de025f2332953a8c8730b4f9bf63fefa`. Real-Chrome QA (2026-07-21, Jason `Default`, real Threads page) reloaded the same unpacked extension to `v.0.3.57`; with Action Brief open and one item selected, the content, stage, details, and selected-summary widths all stayed inside the 559px content track, and the active verdict plate matched its tile within 1px.
- 0.3.56 Product field canvas: the action stage workspace and saved-signals inbox now copy the Topic Atlas canvas treatment verbatim — a rounded `overflow: hidden` field on `color.productCanvas` (the `atlasCanvas` gradient grammar with a steel-blue lean, new token) with three blurred drifting aura blobs (`color.auraProductBlue` new + existing amber/violet, reusing the `dlens-atlas-aura-drift` keyframes under a `prefers-reduced-motion` guard) behind a zIndex-1 content layer. Because the canvas clips at its rounded edge with 12px inset padding, the verdict plate can no longer escape the row under any geometry drift; fresh-load measurements show the plate rect equals the selected tile rect to the pixel at 880/720/600 container widths (4-column branch) and in the ≤520px 2×2 branch. A prior report of residual misalignment after viewport resize was traced to the preview harness freezing the plate's 280ms transition in a throttled background tab, not to source geometry. Stage interior de-flattening: the 觀察原因/新知保留/下一步 beats, park/insufficient reason panel, brief toggle, exact-quote block, and inbox pipeline cells move from flat `contextSurface` boxes to layered material — `elevated`→wash gradients, `cardEdge` hairlines, `shadow.card` inset-highlight elevation, and 12px radii. `tokens-intent.md` records the intent within its line cap. Gate: 1326 tests / 1321 pass / 5 skipped / 0 fail; typecheck, both boundary guards, storage seam guard, diff check, and production build pass. `output/chrome-mv3/manifest.json` is `DLens v3` `0.3.56` with SHA-256 `6cf29cd8154a4c404f39c0c8480c0e0278a5ebb5fa8a6d32e438ecbc1eb46fe5`; the manifest-referenced `output/chrome-mv3/content-scripts/threads.js` is 891579 raw bytes with SHA-256 `2008f0321fa88f8ec3c938d0ba048b525f66193e2e587af51b0fa25cd6b49fa9`. Static gates and the built artifact are release evidence only; reload and manual QA in Jason's `Default` profile remain pending.
- 0.3.55 Product verdict-tile + inbox fix: the Action verdict selector's active plate is absolutely positioned with `width`/`translateX` `calc()` that silently assumed `box-sizing: border-box`, but the workspace sets no global reset, so under the default `content-box` the plate's 1px border made `translateX` overshoot ~8px and the rightmost tile (default-selected `watch`) visibly "flew out" past the row — worse on narrow side panels where 8px is a large fraction of a tile. Restoring 0.3.52 verbatim would not have fixed it (the same latent `calc()` lives there); the plate now scopes `box-sizing: border-box` to `.dlens-verdict-tiles *`, so the plate lands exactly on its tile (verified: plate rect == tile rect, no container overflow). Transparency/rounding: the Action stage card and the Saved Signals inbox frame were flat opaque `elevated` white; they now use the same `atlasPaper` + `atlasBlur` glass material (via a new `glassCardStyle` helper reusing existing tokens — no new palette) at the 20px workspace radius, matching the atlas/hero surfaces. The inbox pipeline is now a four-cell scoreboard and each management row carries a lifecycle-tone dot (green/amber/muted) instead of a bare stamp, so it reads as product language rather than a plain list; refinement-only, no new surface/copy/dependency added. Gate: 1326 tests / 1321 pass / 5 skipped / 0 fail; typecheck, both boundary guards, storage seam guard, diff check, and production build pass. `output/chrome-mv3/manifest.json` is `DLens v3` `0.3.55` with SHA-256 `5be0a16aee3e4c5acf12b23e641d0e7db9394f23209e4a16632d14ed9a1c579b`; the manifest-referenced `output/chrome-mv3/content-scripts/threads.js` is 888621 raw bytes with SHA-256 `4722d87267b25453067a227c4fc1065db21ef12102db0728bf96e0c1c7f5bf08`. Static gates and the built artifact are release evidence only, not real Chrome/Threads acceptance; reload and manual QA in Jason's `Default` profile remain pending.
- 0.3.54 Product Verdict Intake makes Saved Signals a compact lifecycle intake and moves all reading, review, and export ownership into Action. Action restores four visible color-and-text verdict tiles above one keyed stage with a text pager; `park` and `insufficient` retain verdict-specific semantic content without brief, reading, or review controls. The selected brief shelf and the one whole-folder HTML/JSONL packet shelf are Action-only; Saved renders no export UI. Gate: 1326 tests / 1321 pass / 5 skipped / 0 fail; typecheck, B-01/B-04/B-06/B-07/B-08 code-path audit (5/5), both boundary guards, storage seam guard, migration fixtures, diff check, production build, and bundle guard pass. Bundle: 887801 raw / 250270 gzip -9 / 199667 brotli bytes (limits 910000 / 256000 / 203000). `output/chrome-mv3/manifest.json` is `DLens v3` `0.3.54` with SHA-256 `6217d1018f2f1c5ceae466e2412d0ec7f9adc39bcf803483d4f13ed4cd187e67`; the manifest-referenced `output/chrome-mv3/content-scripts/threads.js` SHA-256 is `f445adf5d147d2224e76358430966a5d854105b12c1bdcd4074c0b4e7155cb4b`. Static gates and the built artifact are release evidence only, not real Chrome/Threads acceptance; reload and manual QA in Jason's `Default` profile remain pending.
- 0.3.51 整合-stage truncation root cause: 0.3.50's ceiling raise did not stop the failure because the cause was not budget but `responseJsonSchema` — added to Google topic-audit requests in 0.3.49 (`31772c4`), it drives gemini-3.1-flash-lite into runaway constrained decoding (replayed with the real 19-post 失業 prompt from storage: `MAX_TOKENS` at 6000/9000/16000 ceilings, 31k chars without closing the JSON; `thoughtsTokenCount` 0, so thinking was not the cause). The identical request without the schema finishes `STOP` within the prose budget and parses. Fix: the Google branch of `requestJsonResponse` no longer sends `responseJsonSchema` for topic-audit envelopes (comment + `tests/provider-runtime.test.ts` lock it); envelope shape stays enforced by `parseAuditPromptEnvelopeResult` + the single repair retry, and OpenAI keeps `json_schema`. Verified by driving the real `generateTopicAuditEnvelope` against live Gemini with the stored prompt: attempt 1 success, prose consistent with the last healthy report's section lengths (~220–350 chars/section). Diagnosis used the Playwright LevelDB-copy harness to dump `chrome.storage` state; key-bearing scratch copies were deleted after. Gate: 1290 tests / 1285 pass / 0 fail / 5 skipped; typecheck / boundary / storage seam / build / diff check green; built manifest reports 0.3.51 and the bundled topic-audit generationConfig is schema-free. Runtime QA pending manual extension reload.
- 0.3.50 Topic page de-duplication + large-topic generation fix: the legacy 源清單 manifest section duplicated the atlas 貼文 · 點入單帖 list (same signals rendered twice per page). The topic page now has one source list in the atlas SourceRow grammar, pre- and post-atlas: packet rows gain the LLM signal gist as a brief title plus stance chip, 原文 ↗, 加入比較, and 刪除 quiet actions; uncrawled signals join the same card as `PendingSignalRow` entries (preview snippet, crawl action, delete) instead of living in a second section. The inventory stat strip is removed (crawl progress already lives in the 來源處理 card); `P1 判讀 n/m` appears in the section header only while incomplete. Removed: 源清單 section, `TopicDetailSection` / `TopicInventoryStat` components, `sourcePendingCount` viewmodel field. Generation: the 整合 (final) stage failed `truncated` twice on a 19-post topic because the 7-section report had a 3200-token output ceiling and no length contract; final now gets 6000 output tokens (audience 3200) in `entrypoints/background.ts`, and `buildP6FinalReportPrompt` pins prose to a fixed ~1500-char budget independent of source count, so 100-post topics converge instead of scaling their report length with input. Gate: 1290 tests / 1285 pass / 0 fail / 5 skipped; typecheck / boundary / storage seam / build / diff check green; built manifest reports 0.3.50 and the bundle carries `data-source-row-title` / `data-pending-signal-crawl` with 源清單 absent. Runtime QA pending manual extension reload.
- 0.3.49 Topic session envelope-resilience release: full Topic Audit runs persist typed request ownership and renewable 15-minute leases, checkpoint through the existing serialized mutation queue, reject stale owners, and publish report/episodes atomically only for the live owner. Provider envelopes classify `empty` / `truncated` / `schema_mismatch`; invalid successful responses get exactly one typed semantic retry (two semantic attempts maximum), while transport retry remains separate. Google requests use the keyless endpoint with `x-goog-api-key`, and envelope traces exclude keys, prompts, and raw output. Topic source truth now hydrates into one priority-ordered session state rendered by one owner card and one folder-scoped status rail; duplicate crawl/generation controls were removed. Review fixes lock strict timestamps, complete-JSON precedence, fresh lease checks after provider waits, truthful first-run/failure copy, prior-Atlas preservation across failed reruns, accessible progress semantics, and persisted-report alignment. Static gate: 1289 total / 1284 pass / 0 fail / 5 skipped; typecheck, both boundary guards, storage seam guard, migration fixtures, fixture harness, production build, bundle guard, dual-manifest verification, and diff check pass. Final release bundle: 909846 raw / 255007 gzip -9 / 202909 brotli bytes, within unchanged limits 910000 / 256000 / 203000. Both built MV3 manifests report `DLens v3` `0.3.49`. Source/build 0.3.49 is static-ready only: Default Chrome reload/manual acceptance was not run because the owner explicitly required skipping Chrome/CDP, so this is not runtime proof or runtime-green. Repository state remains local-only, not pushed or tagged.
- 0.3.48 error-free/storage-integrity release: backend JSON requests now have bounded lifetimes (30 seconds by default, 3 seconds for health, 120 seconds for advanced metrics) while preserving caller abort reasons and one terminal trace. Topic evidence, memos, reports, episodes, calibrations, publication, and clear mutations share one recoverable serial queue so whole-map writes cannot silently overwrite one another. Snapshot/global caches, broadcasts, and latency publication now happen only after durable storage succeeds. Signal deletion removes directly owned readings, Product-agent feedback, Topic readings, folder synthesis, Product analysis, and orphaned item tags while retaining shared items/tags plus Topic audit and saved-analysis history. Wave 3 also coalesces/deduplicates hover work, gates closed-popup projections and Technique reads, memoizes Topic summaries, and suppresses identical backend-state setters. Static gate: 1242 tests / 1237 pass / 0 fail / 5 skipped; typecheck, boundary, storage seam, migration fixtures, fixture harness, production build, bundle budget, and diff check green. Both built MV3 manifests report 0.3.48. Real-Chrome reload/manual QA is intentionally pending for the user; this static gate is not presented as runtime proof.
- 0.3.46 Topic truth fix: Topic coverage now separates current inventory from crawled evidence, P1 readings, and the last report snapshot; removed/stale evidence cannot inflate completion and pending never counts as complete. A topic with an existing Atlas exposes `開始爬取 N 篇` whenever current inventory still contains uncrawled sources, while retaining the previous Atlas. Per-post P1 regeneration no longer deletes the completed report or clears unrelated P1/lens caches: the last publication remains visible as stale until a complete audit atomically replaces report + episodes. Topic → Product → Topic switching resets transient audit ownership immediately, ignores late responses, and guards a whole UI audit request with a 15-minute timeout so generation controls cannot remain permanently stuck. Static gate: 1156 tests / 1151 pass / 0 fail / 5 skipped; typecheck / boundary / storage seam / build / diff check green; built manifest reports 0.3.46. Real-Chrome QA in Jason's Default profile loaded the same extension ID from `output/chrome-mv3`, reported `v.0.3.46`, retained the stale 6/7 Atlas, and exposed `開始爬取 1 篇`. The backend then completed the real source crawl with 84 comments and reached idle with zero running/failed analysis, but the extension did not reconcile that terminal result: Topic remained 6/7 with one source marked processing/unfetched and its drawer still showed 0 comments. The extension also issued 4 `/worker/status` requests in 10 seconds. Therefore 0.3.46 is not runtime-green and remains blocked on the planned S7 polling/reconciliation slice; no replacement Topic audit was generated.
- 0.3.47 S6+S7 release: S7 ships the single-poller status sync — `sendAndSync` keeps one stable callback identity so the processing coordinator owns one 12-second idle poller (regression-locked at five `/worker/status` requests per minute), terminal `session/refresh-all` reconciliation stays reachable, and the status rail projects `Backend slow` / `Backend unreachable` instead of a stale `idle`; the backend pairs this with a bounded 1–4 connection pool, 3-second connect/acquire/statement timeouts, and an honest HTTP 503 when the database is unavailable. S6 makes PR narrative temporal validation two-tier — unambiguous corpus-delta language stays a hard reject while bare change/recency words become soft flags adjudicated in context by one semantic repair call; hard rejects that survive repair fail with the offending ref, author, URL, sentence, and matched fragment, and Stage A/B logical calls, chunk labels, repair attempts, and per-attempt HTTP requests land in the QA trace. Gate: 1164 pass / 0 fail / 5 skip; typecheck / boundary / storage seam / build / diff check green; backend 146 pass / 6 skip / 1 xfail. Real-Chrome QA (2026-07-16, Default profile, real Threads page): the service worker updated 0.3.46 → 0.3.47, a formal 60-second window measured exactly 5 `/worker/status` + 5 `/health` requests, stopping the backend walked the visible status through reachable → slow → unreachable with a real `Failed to fetch` in DevTools, and restarting it auto-recovered to `Backend reachable` in 13 seconds without a reload; bundle hashes before and after the gate were identical. The 8 historical dead-letter jobs (4 April `BaseException` handler bugs, 1 missing Playwright browser, 2 dead-session `empty_crawl_result`, 1 smoke-run cleanup; zero downstream crawl_results, zero successful sibling jobs) were verified useless and deleted on 2026-07-16, so `/worker/status` now reports `dead_jobs: 0`. Remaining open item: the real 8-post PR narrative campaign trace (objective LLM call count) is the acceptance gate for the next PR-surface work, not a 0.3.47 blocker.
- 0.3.45 model-transport fix (release-blocker): retires two dead/expiring model ids in `src/compare/provider.ts` — Google `gemini-3.1-flash-lite-preview` → stable `gemini-3.1-flash-lite` (the only actively configured provider), and Claude `claude-3-5-sonnet-latest` (3.5 Sonnet retired 2025-10-28) → `claude-sonnet-5`; OpenAI `gpt-4.1-mini` is still supported and unchanged. Because Sonnet 5 rejects a non-default `temperature` (400) and defaults to adaptive thinking that would consume the small `max_tokens` budgets, all ten Claude request bodies (8 in `provider.ts`, 1 each in `product-context.ts` / `product-profile-init.ts`) drop `temperature` and send `thinking: { type: "disabled" }`. `fetchWithRetry` now honours a provider `Retry-After` delta-seconds header (capped 10s) on 429/5xx instead of only the fixed 250/500ms ladder. No storage, ViewModel, boundary, or report-semantic change; the `-preview` model strings that remain in test fixtures are historical trace data, not live constants. Gate: 1151 tests / 1146 pass / 0 fail / 5 skipped; typecheck / boundary / storage seam / build / diff check green; built manifest reports 0.3.45. Real-Chrome QA: 17 live stable-Gemini calls during a Topic Audit regeneration, all HTTP 200, zero provider errors.
- 0.3.44 adds a 中/英 interface-chrome language switch and re-separates the mode accents. A new Settings 語言 group (`data-settings-language-switch`, persisted via `LayoutPreferences.language`, default `zh`) toggles the shell chrome — mode-header title/deck across Topic/Product/PR/Settings/Collect/Inbox plus the nav rail's ten labels — between 繁中 and English through a React `LanguageProvider` in the popup shell; deep view bodies stay 繁中 for now, and the switch applies on save. The Topic header gains the English `Topic mode` kicker so it matches the other modes. Product mode's accent is restored to its original steel blue (`#234f7a`), retiring the 2026-07-06 vermillion, so the three top-tab modes now read sage Topic / steel-blue Product / rose-red PR — PR the sole red; the change lands in `tokens.ts`, `modeThemes`, and the content-script overlay palette, with `tokens-intent.md` amended in the same pass. Gate: 1151 tests / 1146 pass / 0 fail / 5 skipped; typecheck / boundary / storage / build / diff check green; built manifest reports 0.3.44. Runtime QA requires a manual extension reload.
- 0.3.43 adds a current-only PR Narrative lens beside the existing Evidence lens. Campaign settings now include editable core narrative, target audience, and desired action fields; one setup-generation envelope fills both criteria and narrative settings. A two-stage LLM read first examines each manually collected campaign main post, then synthesizes 2–4 source-linked claims with one priority implication and optional counterexamples. Generation is manual only: there is no automatic post discovery, monitoring, temporal delta, or inferred distribution. Reads persist separately from evidence rows, stale hashes keep the last successful read visible, and every support/counterexample opens the original Threads post. Source publication revalidates inside the shared storage write lock, and publication prose rejects unsupported counts/fractions. The Settings folder-mode radio is also removed from the visible drawer; existing storage shapes remain backward compatible. Gate: 1149 tests / 1144 pass / 0 fail / 5 skipped; typecheck / boundary / storage / migrations / fixture / build / bundle markers / diff check green; both built manifests report 0.3.43. The active real-Chrome profile still exposed 0.3.42, so 0.3.43 runtime QA requires a manual extension reload.
- 0.3.42 corrects Product Saved Signals to follow the approved card-arrival rule per visible signal instead of using the nearly invisible dense-row profile. The list frame stays structural while each signal gets the `0.28 → 1` / `10px → 0` / `480ms` fade-up. Scan rows no longer set an inline transition that overrides the shared tactile transform and shadow spring, so Product hover now interpolates instead of snapping. No backend, storage, ViewModel, or report-semantic change.
- 0.3.41 promotes the card-arrival hierarchy from a Topic exception to a product-wide motion rule. On every Topic, Product, PR, Collect, Library, Casebook, Settings, Compare, Technique, and Result surface, the first standalone top-level card keeps the light soft-pop; subsequent rounded cards wait `70ms`, fade from `0.28`, rise `10px` over `480ms`, and stagger by `60ms` without scale or overshoot. Shared `SurfaceCard` adopts the rule by default; nested cards inherit the parent entrance and dense rows retain their quieter profile. Native scrolling is never replaced or prevented. Route changes and full internal-surface replacements restart the hierarchy once; stale observer callbacks and live reduced-motion changes cannot replay a card or create a second lead. No backend, storage, ViewModel, or report-semantic change.
- 0.3.40 separates arrival motion by information hierarchy. The first Topic surface keeps the approved soft pop; later marked cards wait briefly, fade in from `0.28`, and rise `10px` over `480ms` without scale or overshoot, while dense rows retain their quieter profile. Native trackpad scrolling remains untouched, presence runs once, and reduced-motion users get a static settled layout. All modes keep one stable but tighter workspace height (`min(78vh, 780px)` instead of `min(86vh, 860px)`); the Topic source drawer now fills the actual remaining frame and scrolls its own body. Bottom breathing room has one owner: the shared track keeps a `28px` real spacer after its `12px` gap, while Product, PR, and Result remove duplicate route padding. No backend, storage, ViewModel, or report-semantic change.
- 0.3.39 Topic motion correction keeps native trackpad scrolling and moves the visible rhythm to real rounded surfaces: Topic card → Atlas hero → compass → source card → reliability card (plus the empty-Atlas/source path). Topic cards now fade from `0.68`, rise `10px`, overshoot by `1.5px`, and settle over `420ms`, with an `80ms` same-edge stagger. Inner reaction/narrative legend rows no longer borrow the heavy card shadow or presence animation; they keep a quiet rounded background response. One-in-one-out: nested row motion leaves as the card hierarchy enters. No backend, storage, ViewModel, or report-semantic change.
- 0.3.38 approved motion-presence slice: explicitly marked cards and dense rows across Topic, Product, PR, Collect, Compare, Library/Casebook, and Settings reveal once as they cross the popup viewport (`0.84/0.92 → 1`, `4px`, `240/220ms`, capped `35ms` stagger). Scrolling past the true bottom of a scrollable workspace moves only a dedicated inner track by at most `3px`, then settles over `280ms`; the viewport and scrollbar stay fixed. Hover preset A adds a visible `scale(1.015)` card lift and the same raised treatment to actionable rows. `useWorkspaceScrollMotion` owns reduced-motion, no-observer fallback, cleanup, and route reset; causal filter/reorder motion cancels unfinished presence before taking `translate` ownership. No backend, storage, ViewModel, or report-semantic change.
- 0.3.37 causal-motion UI slice: Topic cards drop the duplicated status-colour side spine and use the mono status kicker as the single report-state encoding. Popup open now performs one masthead → rail → main cascade; actionable cards and dense rows share lift/press semantics; Product Saved Signals, Reading Review, Classification, Action filters, and the Topic list use reduced-motion-safe list choreography only when filtering, expanding, or reordering changes real state. Static Product/PR rows no longer inherit false hover/lift affordances. Topic Atlas/detail triggers retain active-source identity while the single drawer opens. No per-card scroll reveal or `IntersectionObserver` was added.
- 0.3.36 Motion foundation + Topic Audit continuity hardening (refactor + latent-bug fix, no new surface): every `@keyframes` lives in one owner module `src/ui/motion.ts` (`DLENS_KEYFRAMES_CSS`) — including the Signal Atlas drift/pulse, which moved out of `TopicDetailView` so single-owner is literal (`tests/motion-registry.test.ts` fails on any `@keyframes` defined elsewhere). The registry is injected once per document through an idempotent shared injector (`ensureDlensKeyframes`, one guard id) across three mount contexts (threads overlay, in-page collector, audit-report page); the popup hook previously re-injected a second copy of the registry into the same Threads document — that duplication is gone. A `prefers-reduced-motion` safety net travels with the registry (scoped to `[data-dlens-control="true"]`), so every `animation:` callsite — the audit-report skeleton shimmer, collector/overlay spinners and pulses, Atlas drift — is neutralised under reduced motion without per-callsite media queries. The earlier `dlens-success-pulse` name collision stays fixed as distinct `dlens-success-ring` (Product "recently filed" row flash) and `dlens-success-pop` (collector success dot) via `color.successFlashStrong/Soft`; `DLENS_MOTION_CSS` lives in `motion.ts` (re-exported for the threads content script + tests). Compare technique-view switching now scrolls the DLens workspace viewport (`[data-workspace-popup-scroll="viewport"]`), not the host Threads page, and honours the shared reduced-motion scroll preference (`resolveMotionScrollBehavior`). Backend: Topic Audit narrative continuity falls back to the latest episode's `stateSnapshot` when a single-signal P1 regeneration has deleted the report, so claim ids never restart at `claim-1` for a different proposition; same-fingerprint episode revisions retain retired-claim deltas. `dlens-glow-border` / `dlens-scan` remain but are unreferenced. No View/ViewModel boundary or storage seam change; the semantic primitive API (lift/press/presence) is still deferred to the motion mockup.
- 0.3.35 Topic Audit continuity release: evidence aliases now resolve through stable captured-source anchors; P0.5/P1 reuse is keyed by per-signal content/reference identity plus the exact producer/model contract, so appending one signal only pays that signal's shard/post reads while aggregate P2-P6 lenses rerun. A bounded 4096-character `NarrativeState` carries evidence-reanchored claims, voices, and open questions across runs; the latest 24 first/advance/rebase episodes persist beside the report and drive the in-Atlas `本次 / 自上次` delta strip. Report, memo, evidence, and episode revisions are guarded against mixed reads; report+episode publication is one serialized write. The current compass remains instead of copying a donut: reaction-pattern counts are overlapping assignments, not mutually exclusive distribution slices. P2-P6 inputs still grow with the distilled signal set, and P7 remains unwired.
- 0.3.34 Topic Audit bounded-P1 slice: P0.5 now persists each shard's blank-read prose on `CommentShardReading`; both the full audit and single-signal entry point create missing shard readings before P1. P1 is a post reducer over bounded OP context, OP-authored context, capture gaps, and P0.5 prose + structured hints, with a hard 24,000-character prompt ceiling and no raw audience-reply re-render. The 1,000-comment regression fixture spans the first and last shard, preserves the JSON response contract, and rejects every `RAW_REPLY_` marker. This slice does not add cross-run incremental caching, NarrativeState/episodes, P2-P6 hard ceilings, or a new reaction-distribution UI.
- 0.3.33 local UI update: topic cards adopt Jason's status-led mockup grammar. A mono status kicker opens the card (`READY / BUILDING / QUEUED / FAILED / STALE` + `· N 訊號`, same status voice as the masthead StatusRail; stale keeps the `+n` delta chip; the `TopicAuditStatusPill` stays on Topic detail but leaves the card), the serif title grows to 22px, and a new editorial gist line (`data-topic-card-gist`) renders the first sentence of the audit report's `sections.overall` via a new `TopicAuditSummary.headline` field populated in `useTopicAudit` (falls back to `topic.description`, then 採集批次待整理). The updated date moves into the kicker row; the coverage bar returns as a slim two-tone strip that renders once any source is ready (a partial bar is honest coverage; an empty track is not). Five-state copy assertions in `tests/topics-list-view.test.tsx` updated to the new tokens.
- 0.3.32 local UI removal: the topic card's dashboard strip (four KPI stat tiles + dividers, the `打開 ›` link duplicating the clickable card, the always-on completion bar, and the English `queue clear` chip) is replaced by one flat evidence line above a hairline — `N 訊號 · 已完成 n/N · 更新 MM-DD`, with `處理中 n` / `n 待處理` appearing only when nonzero. The two-tone progress bar now renders only while work is in flight (`data-topic-source-state="working"`); a settled topic shows no bar (tokens-intent refuse-list #1: progress theater). All locked `data-topic-*` attributes stay in the states the tests exercise — zero test edits.
- 0.3.31 local UI update: Product signal pages adopt the glass layout grammar. The `WorkspaceSurface tone="utility"` wrapper around all Product routes is removed (sections sit directly on the glass main frame, marked `data-product-signal-frame`); the readiness strip (真實狀態/分析狀態, `ReadinessPanel`) moves from the muted context-wash card onto the shared `workspaceGlass` hero material via a new `heroPanelStyle` (same material as the Collect capture stage); the Saved Signals board header replaces the Kicker `已存訊號` (which duplicated the page title) with `SectionHeader` 訊號清單 + mono count; filter-tab pills move from the status-dot-reserved `radius.round` to `radius.pill`. Rows, tabs, pending drawer, batch export, and every `data-product-*` test contract are unchanged — no test edits.
- 0.3.30 local UI update: Settings is rebuilt on the glass grammar. The triple surface nesting is flattened — the popup's utility-tone wrapper and the view's own `WorkspaceSurface` are both removed, so the `data-settings-group` cards sit directly on the glass main frame (their locked `cardLg` radius + `topicCard` shadow contract is unchanged). Group headers replace the duplicated Kicker+Stamp pairs (e.g. "Folder"/"Folder", "Connection"/"Live") with one shared `SectionHeader` — Chinese serif title, English mono caption (資料夾類型/Folder, 連線/Connection, API 金鑰/API keys, 產品脈絡/Product); the header `Workspace` stamp and the source-card `Loaded/Ready` stamps (redundant with 已載入/尚未載入) are removed. Folder-type radios get a mode-accent selected state; the storage-usage box becomes a quiet tabular line; product context source cards become one-row grids with a success wash when loaded; inputs move to the shared radius/surface tokens. All `data-settings-*` and product-context test contracts pass without any test edits.
- 0.3.29 local UI update: Collect is rebuilt on the glass grammar. The capture hero becomes a `workspaceGlass` panel (`data-collector-stage="hero"`) with one status dot + destination chip row and a mode toggle that is primary when idle; the duplicated in-card `DLens` wordmark, the header `Idle/Active` stamp, and the second folder-name render are removed (one-in-one-out for the new viewfinder + recent-captures card). The preview area is a two-state viewfinder (`data-collector-viewfinder="empty|preview"`): dashed waiting stage vs. opaque preview card with author/time/snippet — preview metrics stay exclusive to the hover overlay (`data-collector-metric-strip="hover-preview"` contract). The save button renders only when a preview exists; keyboard hints become quiet mono kbd chips shown only while capturing; recent captures move into their own opaque `SectionHeader` card (最近採集) with flat line-separated rows. Untriaged-triage behavior and all `data-collector-*` / `data-topic-triage` / `data-untriaged-*` test contracts are unchanged except the wordmark ban, now asserted in reverse.
- Product Action keeps reading generation/review operations inside the currently paged stage; the optional Folder Packet export stays in a disclosure below it. Saved Signals owns its separate batch-selection flow, and that checkbox state never changes which inline reading is committed open.
- Product Settings includes a Product-only cache reset. It clears derived Product analysis, SignalReading, feedback, and compiled ProductContext storage without deleting saved signals, topics, archive folders, or PR evidence.
- Popup runtime hardening: the React tree is wrapped in a top-level workspace ErrorBoundary, and the content-script runtime fallback remains separate.
- Storage diagnostics: Settings displays local `chrome.storage.local` usage through a background-only `storage/get-usage` message boundary.
- Snapshot write discipline: read-modify-write handlers route through `mutateSnapshot` where possible; documented raw-lock escapes cover extra return metadata, no-write returns, and global-only worker wake writes.
- Behavioral storage contracts cover `session/set-mode` fast/slow writes, `session/refresh-all` no-op writes, non-blocking snapshot broadcasts, and real `mutateSnapshot` serialization.

## Version Lock

For every user-visible `main` update, keep these in sync:

- `package.json`
- `package-lock.json`
- `wxt.config.ts` `manifest.version`
- `src/ui/version.ts` `BUILD_VERSION`
- `tests/manifest-config.test.ts` expected version string

`tests/manifest-config.test.ts` verifies this. Chrome's extension page reads the built manifest version; the popup masthead reads `BUILD_VERSION`.

## Build And Verify

```bash
cd dlens-product-latest
npm run typecheck
npm run storage:seam-guard
npm run boundary:guard
npx tsx --test tests/*.test.ts tests/*.test.tsx
npm run build
```

Expected verification before handing off `main`:

- Source routing contract: `npx --yes tsx@4.22.5 --test tests/product-routing.test.ts tests/page-registry.test.ts tests/inpage-collector-state-split.test.ts` should pass with product rail pages `saved-signals / actionable-filter / collect`
- Full suite: `npx tsx --test tests/*.test.ts tests/*.test.tsx`
- `npm run typecheck` passes
- `npm run storage:seam-guard` reports zero allowlisted bypasses
- `npm run boundary:guard` reports zero View / ViewModel wall violations and zero allowlisted bypasses
- `npm run build` mirrors the unpacked MV3 build to `output/chrome-mv3`
- `output/chrome-mv3/manifest.json` reports the same version as `package.json` and `name: "DLens v3"`
- Bundle sanity check for 0.3.13: `output/chrome-mv3` should contain `data-untriaged-delete` and `data-untriaged-delete-selected`, and Product should still contain `data-product-saved-filter-tabs` / `data-product-merged-classification` without rendering `classification` in the Product rail. Manifest version alone is not enough.

## Second Mac Install

For a 30-minute assisted install on another Mac, use [`docs/setup/second-mac-30-minute-install.md`](./docs/setup/second-mac-30-minute-install.md). It is written for someone using Terminal with help from a simple AI chatbot.

## Active Paths

| Purpose | Path |
|---|---|
| Active extension worktree | `dlens-product-latest` |
| Load unpacked extension | `output/chrome-mv3` |
| Chrome QA profile | Jason `Default` profile (`jason@brandonproject.co`); this is the only local Chrome profile with DLens installed from `output/chrome-mv3` |
| Backend (resolved at) | sibling `../dlens-ingest-core` or `DLENS_INGEST_CORE_DIR` |
| Backend repo | `github.com/jasonmaxxxon/dlens-ingest-core` (private) — clone as a sibling; see its `SETUP.md` |
| Backups | `~/dlens-archive-<date>/` (extension + backend bundles + RESTORE.md) |

## Architecture Boundary

- The extension does not connect directly to Supabase.
- Runtime only depends on `settings.ingestBaseUrl`; a local backend checkout is optional for extension-only development.
- Backend analysis snapshots are the source of truth for crawl output and deterministic clustering.
- Extension-side AI calls use the user's local Google/OpenAI/Claude key and must degrade cleanly when no key is configured.
- Product mode must stay insight/evidence/task-first; backend clusters are support data, not the user-facing abstraction.
- View modules must not own extension messaging, raw storage access, or nondeterministic time/random sources; ViewModels must not own browser APIs, network calls, DOM globals, file constructors, or React imports. `npm run boundary:guard` enforces both walls.

## Where To Continue

Read these before non-trivial work:

- [`docs/architecture/dlens-current-architecture-map.md`](./docs/architecture/dlens-current-architecture-map.md) for the live architecture/status map. Treat 🟢 as built, not locked; update the map if a node/edge status changes.
- [`AGENTS.md`](./AGENTS.md) for process rules, current contracts, and agent handoff notes.
- [`docs/handoff/2026-06-18-visual-reset-A-plan.md`](./docs/handoff/2026-06-18-visual-reset-A-plan.md) for the Visual Reset A PR sequence and single design-source contract.
- [`docs/ENGINEERING_PLAN.md`](./docs/ENGINEERING_PLAN.md) for the completed N1-N5 engineering-plan slice and deferred-trigger pool.
- [`docs/CODE_REVIEW.md`](./docs/CODE_REVIEW.md) for the current PR self-check contract.
- [`docs/memory/current-state.md`](./docs/memory/current-state.md) for the fuller repo state.
- [`docs/memory/latest-shared-context.md`](./docs/memory/latest-shared-context.md) for Codex/Claude shared memory context.

Current state and open risks:

- `entrypoints/background.ts` is 3488 lines; do not split handlers unless the trigger in `docs/ENGINEERING_PLAN.md` promotes that work into the committed-next slice.
- `src/ui/useInPageCollectorAppState.ts` is 2148 lines; continue extraction before adding more product/PR/export routes.
- C-Backend read-model hardening is now 🟢: B1-B4 landed across backend and extension projection; future read-model changes must update the shared seven-case golden fixtures.
- BOUNDARY is now 🟩: View / ViewModel wall guards are wired into CI through `npm run boundary:guard`.
- VIEW remains 🟢, not 🟩: Visual Reset A marquee surfaces are locked, but row-level primitive adoption / large-view LOC reduction is still follow-up work.
- Chrome QA must verify the rebuilt `output/chrome-mv3` in Jason's `Default` profile. A source fix plus a bumped manifest can still show stale UI if the bundle was not rebuilt or the unpacked extension was not reloaded.
