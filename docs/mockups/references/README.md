# Mockup References

Reference only. The active Visual Reset A contract is `src/ui/tokens.ts`: warm editorial content language plus macOS utility shell patterns inside the existing token system. These artifacts may inform motion or shell details, but they are not a competing design source.
